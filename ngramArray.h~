#ifndef NGRAMARRAY_H
#define NGRAMARRAY_H

#include <stdio.h>

typedef struct {
char *ngram;
double szansa;
double pmi;
ngramNext *nextArray;

} ngramElem;

typedef struct {
*ngramElem wskaznik;
double szansa;
} ngramNext;
#endif
