#ifndef GENER_H
#define GENER_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "./ngramTree/tree.h"

void printConnectionsStat(node * tree, int rng, int lvl, int lim);
void statystyka(node * tree, int rng);



#endif
